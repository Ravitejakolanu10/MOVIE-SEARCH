<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="styles/style.css">
    <title>Movie Search</title>
</head>
<body>

    <nav class="navbar bg-body-tertiary">
        <div class="container-fluid">
          <form class="d-flex" role="search">
            <input class="form-control me-2 search" type="search" placeholder="Enter Movie Title" aria-label="Search" name="title">
            <button class="btn btn-outline-success" type="submit">Search</button>
          </form>
        </div>
      </nav>

    <div class="container">
        <% if (movie && movie.Search && movie.Search.length > 0) { %>
            <h1>Results For '<%= movie_name %>'</h1>
            <h2><%= movie.Search[0].Title %> (<%= movie.Search[0].Year %>)</h2>
            <p>Type: <%= movie.Search[0].Type %></p>
            <p>IMDB ID: <%= movie.Search[0].imdbID %></p>
            <img src="<%= movie.Search[0].Poster %>" alt="<%= movie.Search[0].Title %> Poster">

            
        <% } else { %>
            <p style="color: red;"><%= error || 'No results found.' %></p>
            
        <% } %>
    </div>      

    
    
    













   
      
</body>
</html>